//todo primitive data type (call by value) 

// *1. number, string, boolean, null, undefined, symbol, bigint

//*JavaScript dynamically typed language hai.

const score = 100;

const isLoggedIn = false;

const outsideTemp = null;

let userEmail; //undefined

const id = Symbol("123");

const bigNumber = 3456789012n;

//todo reference data type mean k non primitive (call by reference)

//* Array , object and function


const name = ["aman", "raj", "rohit"];

const person = {
    name: "aman",
    age: 22,
};

const myFunction = function(){
    console.log("hello world");
}
